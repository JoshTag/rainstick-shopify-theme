<style>
.hero__container {
  background-repeat: no-repeat;
  background-size: cover;
  background-position: center;
/*   height: 700px; */
  position: relative;
  display: flex;
  flex-direction: column;
}
.hero__container:after {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(270deg, rgba(0, 0, 0, 0.89) 24.95%, rgba(0, 0, 0, 0) 51.75%);
  transform: rotate(180deg);
}
.hero__content--top,
.hero__content--bottom {
  position: relative;
  z-index: 1;
}
.hero__content--top {
  padding-top: 150px;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.hero__content--top h1,
.hero__content--top p {
  color: #F7EDE2;
  text-align: center;
  width: 320px;
}
.hero__content--top h1 {
  font-size: 26px !important;
  font-weight: 300;
}
.hero__content--top p {
  font-weight: 400;
}
.hero__content--bottom {
  margin-top: auto;
}
.hero__buttons {
  margin-top: 40px;
  width: 320px;
  padding: 10px;
}
.hero__buttons a {
  width: 100%;
  display: block;
  text-align: center;
  font-weight: 500;
  height: 50px;
  line-height: 50px;
  margin-bottom: 10px;
  border-radius: 6px;
  border: 2px solid #02827B;
}
.hero__buttons a:first-of-type {
  background-color: transparent;
}
.hero__buttons a:first-of-type:hover {
  background-color:#02827B;
}
.hero__buttons a:last-of-type {
  background-color: #02827B;
}
.hero__content--bottom p {
  color: #F7EDE2;
  text-align: center;
  font-size: 18px;
  font-weight: 600;
  display: flex;
  justify-content: center;
  align-items: center;
}
.hero__content--bottom p br {
  display: none;
}
.hero__featured-logos {
  display: flex;
  justify-content: space-between;
  flex-wrap:wrap;
  padding:0;
}
.hero__featured-logos{
    background-color:rgb(0 0 0 / 68%);
  }
@media screen and (max-width: 768px) {
  .hero__featured-logos{
    padding:1rem;
  }}
@media screen and (min-width: 768px) {
  .hero__content--top {
    align-items: unset;
    width: 100%;
    margin: 0 auto;
    padding: 150px 60px 0;
  }
  .hero__content--top > h1,
  .hero__content--top > p,
  .hero__content--top > div {
    width: 100%;
    text-align: left;
  }
  .hero__content--top > h1 {
    font-size: 60px !important;
    font-weight: 200;
    line-height: 74px;
    padding-bottom: 1rem;
  }
  .hero__content--top p {
    padding: 5px 0;
    width: 100%;
  }
  .hero__buttons {
    margin-top: 20px;
    padding: 0;
  }
  .hero__buttons a {
    display: inline-block;
    width: 200px;
  }
  .hero__buttons a:last-of-type {
    margin-left: 20px;
  }
  .hero__content--bottom {
    display: flex;
    flex-direction: row;
  }
  .hero__content--bottom p {
    text-align: left;
    width: 150px;
    padding-left: 1rem;
  }
  .hero__content--bottom p br {
    display: block;
  }
  .hero__featured-logos {
    max-width: unset;
    width: 100%;
    margin: 0;
    justify-content: unset;
  }
}
@media screen and (min-width: 900px) {
  .hero__container {
    height:100vh;
  }
  .hero__content--top {
    padding: 200px 100px 0;
  }
}
</style>

{%- capture background_image -%}
{%
  include 'rimg',
  img: section.settings.background,
  size: image_size,
  alt: "Rainstick shower at night",
  background: true
%}
{%- endcapture -%}

<div class="hero__container" {{ background_image }}>
  <div class="hero__content--top" style="position:relative;">
      <div class="hero-blob"></div>
    <h1>More Showers,<br> Less Guilt</h1>
    <p>2x the flow rate with 80% water and<br>
      energy savings. Now that is guilt free comfort!</p>
    <div class="hero__buttons">
      <a href="/pages/select-destination">ORDER NOW</a>
      <a href="/pages/how-it-works">MEET RAINSTICK</a>
    </div>
  </div>
  <div class="hero__content--bottom">
    <p>They Really<br> Like Us!</p>
    <div class="hero__featured-logos">
      {%- render 'rainstick-media-logos', name: 'CES', icon_class: 'hero-logo' -%}
      {%- render 'rainstick-media-logos', name: 'Toronto Star', icon_class: 'hero-logo' -%}
      {%- render 'rainstick-media-logos', name: 'Forbes', icon_class: 'hero-logo' -%}
      {%- render 'rainstick-media-logos', name: 'IDS', icon_class: 'hero-logo' -%}
      {%- render 'rainstick-media-logos', name: 'Canie', icon_class: 'hero-logo' -%}
      {%- render 'rainstick-media-logos', name: 'Style at Home', icon_class: 'hero-logo' -%}



    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Hero",
    "settings": [
	  {
      "id": "background",
      "type": "image_picker",
      "label": "Background image"
      }
	],
    "presets": [
      {
      "name": "Hero"
      }
    ]
  }
{% endschema %}
